services:
  - type: web
    name: sanfen-webhook-v10
    runtime: docker
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ALLOWED_CHAT_ID
        sync: false
      - key: DB_PATH
        value: history.db
